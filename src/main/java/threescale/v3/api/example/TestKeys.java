package threescale.v3.api.example;

/**
 * Edit this interface and replace the values with your live values to test the calls to the backend.
 */
public interface TestKeys {
    static String my_provider_key = "<provider key>";

    //App Id
    static String app_id            = "<app id>";
    static String app_key           = "<app key>";
    static String app_id_service_id = "<app id service id>";

    //API Key
    static String user_key            = "<user key>";
    static String user_key_service_id = "<user key service id>";

    // OAuth
    static String oauth_service_id = "<oauth service id>";
    static String oauth_app_id     = "<oauth app id>";
}
